function sameFrequency(int1, int2){
    // deal with edge cases
    let frequency = {};
    let str1 = int1.toString();
    let str2 = int2.toString();
    if (str1.length !== str2.length) {
        return false;
    } 
    for (let digit in str1) {
    }
  }

  // input: 2 integers; output: boolean